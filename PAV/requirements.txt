# DEPRECATED — pyproject.toml이 canonical. 이 파일은 비-uv 환경 호환용.
# 권장: `uv sync` (base) / `uv sync --extra gpu` (전체) / `uv sync --group dev` (테스트)
#
# 구현 계획서 §6-2 핵심 라이브러리 버전
torch>=2.4
transformers>=4.45
trl>=0.11
vllm>=0.6.3
peft>=0.13
autoawq>=0.2.6
accelerate>=1.0
hydra-core>=1.3
wandb>=0.18

# 데이터 / 평가 보조
datasets>=2.20
math-verify>=0.3      # 정답 매칭 (선택)
pyyaml>=6.0
pytest>=8.0
